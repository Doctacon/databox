[project]
name = "databox"
version = "0.5.0"
description = "Databox shared library — config, quality engine, and Dagster orchestration."
requires-python = ">=3.12"
dependencies = [
    "databox-sources",
    "pydantic>=2.10.4",
    "pydantic-settings>=2.13.0",
    "python-dotenv>=1.0.1",
    "pyyaml>=6.0",
    "soda-duckdb>=3.0.0",
    "dagster>=1.5.0",
    "dagster-webserver>=1.5.0",
    "dagster-dlt>=0.1.0",
    "dlt[duckdb]>=1.14.1",
    "duckdb>=1.5.3",
    "sqlmesh>=0.204.0",
    "dagster-sqlmesh",
    "google-adk>=2.4.0",
    "httpx>=0.28.1",
    "fastapi>=0.115.0",
    "uvicorn>=0.35.0",
]

[project.optional-dependencies]
# The upstream `openlineage-dagster` package pins Dagster <=1.6.9, which
# conflicts with dagster-sqlmesh's Dagster >=1.7.8 floor. Stick with the
# pure `openlineage-python` client and emit via a small in-tree sensor
# (see orchestration._factories.openlineage_sensor_or_none).
lineage = [
    "openlineage-python>=1.20.0",
]

[build-system]
requires = ["setuptools>=70.0.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["databox*"]

[tool.dg]
directory_type = "project"

[tool.dg.project]
root_module = "databox"
defs_module = "databox.orchestration.definitions"
code_location_target_module = "databox.orchestration.definitions"
code_location_name = "databox"

[tool.uv.sources]
databox-sources = { workspace = true }
dagster-sqlmesh = { git = "https://github.com/Doctacon/dagster-sqlmesh.git" }
