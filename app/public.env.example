# Run `npm run dev:public` or `npm run build:public`. Public mode uses a separate
# static entry point and never imports or calls the local FastAPI application.
# It has no runtime service URL, API key, Turnstile key, or email configuration.
