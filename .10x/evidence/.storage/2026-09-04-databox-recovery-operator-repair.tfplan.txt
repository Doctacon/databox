REDACTED HISTORICAL PLAN TEXT — NON-EXECUTABLE
Publication redaction (2026-09-12): deployment account/bucket literals are replaced by labeled placeholders; generic principal labels are retained. This historical record is not a fresh approval or an executable plan. Original conclusions and verification limits still apply.
Source revision: `027af8b4271d60ffc193967d092b5d2497af13ad`. Original-artifact SHA-256 (NOT this redacted text): `78b80e6fd2619d40482fb0b4869f37574d5517bc153359a2f4d7eccca9a5dbaa`.
Exact private original: `~/Private/databox/recovery-evidence/2026-09-11T235551Z-05aef1141954/`; `manifest.json` entry with `source_kind=committed-head` and `source_path` equal to this public path. Any preexisting plan/export hashes below identify original artifacts, not this changed counterpart.


OpenTofu used the selected providers to generate the following execution
plan. Resource actions are indicated with the following symbols:
  + create
  ~ update in-place (current -> planned)
 <= read (data resources)

OpenTofu will perform the following actions:

  # data.aws_iam_policy_document.operator_assume will be read during apply
  # (config refers to values not yet known)
 <= data "aws_iam_policy_document" "operator_assume" {
      + id            = (known after apply)
      + json          = (known after apply)
      + minified_json = (known after apply)

      + statement {
          + actions = [
              + "sts:AssumeRole",
            ]

          + principals {
              + identifiers = [
                  + (known after apply),
                ]
              + type        = "AWS"
            }
        }
    }

  # aws_iam_role.catalog_backup will be updated in-place
  ~ resource "aws_iam_role" "catalog_backup" {
      ~ assume_role_policy    = jsonencode(
            {
              - Statement = [
                  - {
                      - Action    = "sts:AssumeRole"
                      - Effect    = "Allow"
                      - Principal = {
                          - AWS = "arn:aws:iam::<REDACTED_ACCOUNT_ID>:root"
                        }
                    },
                ]
              - Version   = "2012-10-17"
            }
        ) -> (known after apply)
        id                    = "databox-polaris-catalog-backup"
        name                  = "databox-polaris-catalog-backup"
        tags                  = {}
        # (8 unchanged attributes hidden)

        # (1 unchanged block hidden)
    }

  # aws_iam_user.recovery_operator will be created
  + resource "aws_iam_user" "recovery_operator" {
      + arn           = (known after apply)
      + force_destroy = false
      + id            = (known after apply)
      + name          = "databox-recovery-operator"
      + path          = "/"
      + tags_all      = (known after apply)
      + unique_id     = (known after apply)
    }

  # aws_iam_user_policy.recovery_operator will be created
  + resource "aws_iam_user_policy" "recovery_operator" {
      + id          = (known after apply)
      + name        = (known after apply)
      + name_prefix = (known after apply)
      + policy      = jsonencode(
            {
              + Statement = [
                  + {
                      + Action   = [
                          + "sts:AssumeRole",
                        ]
                      + Effect   = "Allow"
                      + Resource = "arn:aws:iam::<REDACTED_ACCOUNT_ID>:role/databox-polaris-catalog-backup"
                    },
                ]
              + Version   = "2012-10-17"
            }
        )
      + user        = "databox-recovery-operator"
    }

Plan: 2 to add, 1 to change, 0 to destroy.

Changes to Outputs:
  + recovery_operator_user_arn = (known after apply)
